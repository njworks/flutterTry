import 'package:flutter/material.dart';

const cardMainColour = Colors.lightBlueAccent;
const activeCardColour = Colors.grey;

/// Normal text style
const labelTextStyle = TextStyle(fontSize: 16, color: Colors.white);

/// Big font style for numbers
const labelAdjustNumber = TextStyle(fontWeight: FontWeight.w900, fontSize: 50);

/// Large font style
const labelLargeStyle = TextStyle(
  fontSize: 25,
  fontWeight: FontWeight.bold,
);

/// Title font style
const titleTextStyle = TextStyle(fontWeight: FontWeight.bold, fontSize: 50);

const resultTextStyle =
    TextStyle(color: Colors.pink, fontSize: 22, fontWeight: FontWeight.bold);

const resultsBMIStyle = TextStyle(fontSize: 100, fontWeight: FontWeight.bold);

const resultBMIBodyStyle = TextStyle(fontSize: 22);
